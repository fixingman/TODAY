# TODAY — Changelog

> Entries older than ~20 versions → `Changelog-archive.md`

| Version | Key change |
|---|---|
| **2.90.36** | **Sunday now surfaces a supported insight, not a dressed-up statistic.** The production line *“You closed out 41 of 206 threads…outlived their relevance—not burnout, just expiration”* repeated the two earlier pool failures: numerically grounded, but opaque and inert. The fix is architectural across all six observation kinds. Each candidate now carries separate `evidence` and a code-owned `insight`: the evidence proves the relationship; the insight states the bounded human reading or lever. Sunday sends the model only the selected kind and insight—the evidence counts remain inside deterministic policy and diagnostics—so the model phrases meaning instead of discovering it from statistics. Output retains earned warmth, wit, and a clarifying metaphor when useful; a candidate-aware guard requires the response to preserve the kind's literal actions and relationship, so both the reported sentence and fluent generic filler are rejected. Policy `earned-v4` allows today's older-policy line to be replaced once. The local audit now uses `survivesGate` for every viable candidate and `selected: true` only for the single winner. Tests cover the candidate schema, bounded readings, payload boundary, semantic anchoring, stale-policy replacement, and one-winner diagnostics. |
| **2.90.35** | **Sunday silence now distinguishes repetition from accumulation.** Same-surface per-kind cooldowns remain strict. A different surface may revisit a kind only after 7 days and only when the retained outcome log proves material new evidence since the earlier line (per-kind privacy-safe signals; no additional personal-data snapshot). The gate now evaluates the newest blocker, fixing the audit's misleading September 4 explanation when September 10 was effective. Morning generation is held behind a new initial-memory-ready gate until Dropbox has merged `spokenLines`, closing the cold-start race that could let 2 devices independently speak `soon-pullback` inside its cooldown; synced cached text may still render immediately. The readiness hook is attached to the existing `checkDayNudge` function rather than expanding the global surface. Sunday policy bumped to `earned-v3`; there is still no generic weekly fallback. The local audit now splits observed vs backfilled revives so zero linked returns can be diagnosed without ids or text. Tests cover material cross-surface reopening, unchanged evidence, the 7-day floor, strict same-surface cooldown, newest-blocker reporting, sync readiness, and revive provenance. |
| **2.90.34** | **Sunday silence is now diagnosable without making Sunday noisier.** Added pure `_observationPoolAudit()` over the exact 30/45-day outcome windows, thresholds, Sunday eligibility, novelty cooldowns, and permanent verdicts. It returns only aggregate evidence and kind metadata—never task ids/text, candidate prose, or spoken prose. `_debugSundayAudit(date)` saves a local-only `sunday_observation_audit_<date>` record on Sunday startup, after the existing post-sync re-check, and when About renders; explicit dates allow a Sunday to be reconstructed later while its evidence remains retained. The same report records cache state and AI stages (no insight/key, offline, request, HTTP/empty/rejected/exception, accepted). No UI, network call, sync field, threshold, prompt, or silence policy changed. Pure tests cover sparse, ready, cooldown, and privacy cases; browser tests cover explicit-date storage, AI outcome, and automatic Sunday capture. |
| **2.90.33** | **Dropbox sync: three cross-device memory fixes.** (1) `obligationHistory` merge now OR's the `letgo` flag alongside `done` — a task released on one device was previously invisible to the other. (2) `_lastAbstractDate` merged with max-wins — without this, both devices ran AI abstraction once per day independently, doubling inference generation. (3) AI inference dedup in the Dropbox merger upgraded from 8-char prefix to full normalized-text comparison across all three slots, matching the fix made in `_memoryAbstract` itself. Tests added for all three. |
| **2.90.32** | **`_memoryAbstract` redesign.** Input replaced: pre-computed statistical summaries (completion hours, lifespan, late-add %, focus/habit correlations, DOW pattern) removed and replaced with raw text+outcome data — `recentCompletedTasks` (last 20), `obligationHistory` both done and released (comparable populations, last 20 each), and `returningTasks` (top 15 by dayCount). Parser bug fixed: `ai-assist.js` returns a parsed array directly; the old code passed it through `_parseAIText({content,message})` which always returned null. New items now stored as `status: 'pending'` (not fabricated `confirmed: 0.7`). Deduplication upgraded: full normalized-text comparison across all three slots (8-char prefix check replaced). Prompt-injection defense: records sent as JSON object; system prompt marks all `t` fields as untrusted user data. Output capped at 3 items in code. Test updated: uses real array response shape and asserts `pending` status. |
| **2.90.31** | **Memory panel: three new rule-based items.** EPISODIC: "N tasks started — still open" surfaces returning tasks that already have focus sessions (≥2 threshold). PROCEDURAL: "obligation tasks: X% complete vs Y% overall" compares resolved obligation-framed tasks against the daily history rate (≥8 resolved, ≥5 history days); falls back to absolute count when history is shallow. "1 in N let-go tasks comes back" from 30-day `taskOutcomes` window (≥10 letgo+revive events, ≥15% return rate). |
| **2.90.30** | **Memory panel noise reduction.** Three output-stat items removed from PROCEDURAL: completion rate ("completes 67% of tasks added"), habit cross-variable ("all habits done: X avg vs Y"), and focus cross-variable ("focus days average X vs Y without a session") — these restated productivity counters rather than the user's relationship with commitments. Peak-hour item drops its identity suffix ("a morning person"); SEMANTIC label changed from "stable things today has concluded about you" to "patterns observed over time." |
| **2.90.29** | **Declarative AI-surface registry and deterministic visual gate.** Dated AI text surfaces now share one registry for cache keys, expiry cadence, Dropbox payload fields, spoken-line names, and fallback policy; the existing nudge, Sunday/Monday, week-theme, and focus-question behavior remains unchanged. The visual regression harness now freezes the complete local clock to 2026-09-14, verifies the rendered date, waits for fonts, fails on uncaught page exceptions, closes pages after failures, and takes two captures of every scene—each against the stored baseline and against repeat 1. A deliberate page-error probe proves exceptions fail, and the same baseline passes in both Kiritimati and Los Angeles time zones. The default gate remains 38 checks and passes cleanly with 0 flaky. |
| **2.90.28** | **Focus companion animation polish.** Two fixes: (1) The `✦ ask` high-signal → clicked transition was missing because `_thinkAnim` WAAPI started synchronously and immediately overrode the CSS `opacity` transition; it now starts after 150 ms so the accent → loading fade completes first. (2) Clicking the completed bar to restart snapped the pulsing fill opacity mid-animation; `_pulseComplete(false)` now reads the current WAAPI-driven opacity via `getComputedStyle` before cancelling and runs a 120 ms ease-out to opaque, eliminating the snap. |
| **2.90.27** | **Performance and visual evidence are now executable, budgeted, and self-updating.** `scripts/performance-test.mjs` runs five cold and warm mobile-like loads (4× CPU, 1.6 Mbps, 150 ms RTT) plus five desktop add-task/focus interaction checks, reporting median and worst FCP, LCP, CLS, total blocking time, navigation timing, TODAY lifecycle marks, and response latency. Deterministic payload guards cap the decoded/Brotli shell, index, fonts, and eager script count. Four native marks define `today:morning-ready`, `today:interactive`, `today:shell-visible`, and `today:splash-dismissed`. The completed `visual-test.mjs` suite is now part of the inventory-enforced default gate: stored PNG baselines cover morning empty state, populated tasks, focus, triage, Memory, and the 320px mobile layout. Push CI runs both suites; a scheduled workflow measures the deployed dev URL and retains its JSON artifact. The checked-in JSON baseline generates the current block in `Performance-audit.md`, and the gate fails when app version, payload evidence, budgets, or rendered audit drift. No analytics or runtime reporting was added; measurements stay local or inside GitHub Actions. |
| **2.90.26** | **Habits edit surface cleanup.** Replaced the left-bar edit chrome with persistent `surface2` rows and aligned the edit input with the established box-border pattern used by the habit input. |
| **2.90.25** | **`--panel-max-width` token.** `420px` appeared independently in `#triagePanel`, `#meetingPanel`, and `#undoToast`; now one token. |
| **2.90.24** | **`--add-task-bar-height` token.** Three separate hardcoded values (72px × 1, 70px × 2) all meaning "clearance for the fixed bottom bar" are now one token at 72px: main app scroll clearance, triage bar bottom, and undo toast bottom. |
| **2.90.23** | **Tokenize two hardcoded font-sizes.** Nudge star `11px` → `var(--text-sm)`; meeting review title `15px` → `var(--text-md)` (14px, 1px imperceptible). Design-lint now catches all font-size escapes. |
| **2.90.22** | **Soon and Past section labels brighten on hover.** Added `transition: color var(--dur-fast)` to `.section-label` and `.zone-header:hover .section-label { color: var(--text) }` — title text now travels to full color alongside the expand chevron. |
| **2.90.21** | **Focus companion: high-signal ask button.** `_computeSignalDensity()` counts task-specific context signals (session count, age, recency gap, revived, deferred, drag-word match, dominant letgo pattern) at session open. When ≥3 fire, the ✦ ask button turns accent before the user clicks. Time-of-day excluded to prevent the repetitive "it's late" pattern. Clears on click, reset, and close. |
| **2.90.20** | **Fix: Sunday reflection silent block.** Week theme and Monday intention failed to load when a fresh AI call was needed; the silent block is resolved. |
| **2.90.19** | **Fix: quiet Shift+Space captures no longer invent tasks.** The MediaRecorder fallback previously sent every non-empty audio container to Gemini, including valid blobs containing only silence or room noise; a generative transcriber could answer those with plausible words. Quick capture now samples microphone RMS locally while recording and requires at least 160ms of detected activity above the silence floor before uploading. A key click or ambient hum therefore ends as “Nothing heard,” with no network request and no task. If Web Audio metering is unavailable, the quick-capture request adds a second conservative contract: silence, breathing, handling sounds, and unintelligible background noise must return a no-speech marker, which the endpoint converts to empty text. This request flag is exclusive to Shift+Space, so the separate mobile Voice Note behavior is unchanged. The voice regression now covers the endpoint contract, audible capture, silent capture, local recognition, failure, cleanup, and shortcut guards. No audio, transcript, or activity samples are persisted. |
| **2.90.18** | **Shift+Space now has an explicit capture trust boundary and a true hold lifecycle.** The initial v2.90.17 implementation called legacy `SpeechRecognition` directly; on common browsers that can send microphone audio to a browser-owned server even though TODAY has no configured connection, defaults to one result rather than a continuous held utterance, and stopped only when Space came up while Shift was still held. Quick capture now uses `SpeechRecognition` only when `available({ processLocally: true })` confirms the active language is installed, then sets `processLocally = true`, continuous results, and accumulates final segments until release. Otherwise it records one self-contained 45-second-maximum clip at 32 kbps and sends it through the existing `/transcribe` function using the user's configured Gemini key; no meeting-style chunks, browser-cloud fallback, stored audio, or stored transcript. Either Shift or Space release ends capture, as does window blur. The pill moves through Getting ready → Listening → Finding the words → Added, with visible empty, unavailable, and failure outcomes; its listening dot now uses the shared reduced-motion-aware WAAPI breath instead of a CSS loop. New `voice-capture-test.mjs` covers the local preference, one-blob fallback, modifier release order, guards, media cleanup, success, and failure. The default gate is now 36 non-live suites plus design lint: 37 checks. No storage, sync, provider-selection, or routing interface changed. |
| **2.90.17** | **Shift+Space quick voice capture.** Holding the chord outside a text field starts a compact listening pill; releasing it converts one spoken task through the shared `addTaskFromText` path so URL parsing, memory bookkeeping, sync, arrival motion, and enrichment match typed tasks. The first implementation used the browser's default Web Speech recognizer; v2.90.18 replaced that trust boundary and lifecycle before promotion to master. |
| **2.90.16** | **Three missing lifecycle tests, plus the DST defect one of them exposed.** New `service-worker-test.mjs` runs the real worker rather than a registration stub: a fresh install must control the app and cache all 59 shell resources, activation purges a seeded legacy cache, a byte-distinct update must skip waiting/take control and replace the old cache, and both the cached app shell and branded fallback must load when their network path fails. New `timezone-test.mjs` runs the shared calendar helpers in fresh processes pinned to Los Angeles, Kiritimati, and Kathmandu; it proves one UTC instant maps to each device's local day, both repeated fall-back hours stay on the prior habit day, spring-forward changes at local 3am, and date-line/fractional offsets obey the same boundary. That suite caught `_habitNow()` subtracting three elapsed hours, which moved the habit boundary to 4am after spring-forward and 2am after fall-back; it now subtracts three local wall-clock hours with `setHours`. `focus-test.mjs` now supplies a behavioral Document PiP fake and exercises open-on-hide, task/time/accessibility mirroring, Open, Breathe/Resume, completion→Again, restart, Rest, close, and persisted-session cleanup. The default gate is now 35 non-live suites plus design lint: 36 checks. Real-device PiP, installed-PWA, timezone-travel, and assistive-technology checks remain manual. No storage or sync schema changed. |
| **2.90.15** | **Motion pass: continuity for choosing, reordering, and starting focus.** Evening-reflection choices now preserve the selected whole word through the state change: other choices recede over `--dur-fast`, the selection acknowledges over `--dur-base`, then a fixed clone settles into the confirmation over `--dur-mid`. Task, Trello, and habit reordering now share FLIP motion across mouse, touch, and Option+Arrow: neighboring rows make space with `--ease-out`, the moved row lands with `--ease-spring`, and the touch ghost settles to the final row before disappearing. Starting focus now recedes rows outward from the chosen commitment, mapping visual distance across one `--dur-fast` delay span while leaving the chosen row still. All WAAPI duration/easing values resolve the existing CSS tokens through shared helpers in `assets/util.js`; reduced motion preserves every state change but skips the choreography. Focused regression coverage adds normal/reduced choice motion, all three reorder inputs, and normal/reduced focus waves. |
| **2.90.14** | **Focus companion: overflow questions scroll instead of truncating.** When the AI question is longer than the timer bar, a WAAPI animation scrolls it pendulum-style: 1.8s pause at the start, ease forward to the hidden end, 1.2s pause, ease back, repeat. Scroll duration scales with overflow distance (~35px/s) so a 60px overflow takes ~1.7s per pass, a 200px overflow ~5.6s. `prefers-reduced-motion` skips the animation and leaves the ellipsis. The scroll span is cleaned up in `_focusResetAI` so the "✦ ask" idle state is unaffected. |
| **2.90.13** | **Memory panel: reconstruction caveat removed.** The line *"N of those reconstructed from older history — no focus data on them"* beneath the 30-day outcome count is removed. It was a data-quality footnote about backfilled rows, not information that helps the user understand their commitments. The outcome count line itself (done · let go · to Soon · brought back) remains. |
| **2.90.12** | **Post-triage reflection: countdown progress bar.** `#triageComplete` now shows a 2px progress bar (`#triageCountdown`, class `triage-countdown`) that shrinks left-to-right over the auto-close duration. Duration is set dynamically in `_startCountdown(ms)` so it matches the variable timeout (10 s intro, 6 s question, 8 s after "Remember", 3 s after selection/decline). Pauses on `mouseenter` (`animationPlayState: paused`), resumes on `mouseleave`. `_triageResetAutoClose` also calls `_startCountdown` so the bar restarts when reflection actions extend the timer. `#triageComplete` gains `position: relative; overflow: hidden` to contain the absolutely-positioned bar. |
| **2.90.11** | **How Days Felt: four commitment axes, not two.** `_buildReflectionCandidate` now compares feelings across four types of evening: obligation-framed vs chosen completions (score 110), focus-session days vs non-focus days (105), letting go vs finishing without a release (100), and reviving a task vs finishing without a revival (90). The focus partition excludes backfilled rows (`focusSessions: 0` is unknown there, not absence); both new axes share the same ≥4-evenings, ≥3-appearances, ≥30pp-gap guards. Three new reflections-test cases cover the focus axis, the backfill exclusion, and the revive axis. |
| **2.90.9** | **Fix: revive-from-past reason menu trimmed to two options.** The four-option menu that appears when moving an item from PAST back to SOON reduced to two: "still relevant" and "new context". "Too hasty" was self-referential — the act of reviving already communicates this, so the label added no signal. "Came up again" overlapped heavily with both remaining options: it is either still relevant, or something changed (new context). Existing `too_hasty` and `came_up_again` keys in `appMemory.patterns.reviveReasons` age out naturally within the 90-day window; nothing downstream branches on specific reason values. |
| **2.90.8** | **Poem share image: TODAY wordmark at the top of the canvas.** The 1080×1350 canvas now opens with a small centered mark: the 4-pointed star (same SVG path as the splash screen's `#splash-star`, scaled to 14 px) followed by "TO" in dim `#555` and "DAY" in accent `#c8f060`, in DM Mono 400 at 20 px. Logo unit width is measured then centered so the star and the text sit as one element. |
| **2.90.7** | **Fix: HOW DAYS FELT now offers a relationship with commitments, or stays a record.** The reported 17-evening sentence was both clipped after *"and"* and, before the truncation, only an inventory of feeling frequencies. The proxy caused the clipping by wrapping unstructured model text at 200 characters, but a North Star review showed that shortening the prompt alone would merely polish the deeper failure: frequency tables and focus-duration splits describe a container of data, not the person's relationship with commitments. The old dominant-feeling and focus-association messages are retired. On-device code now temporarily joins reflection dates to date-only task outcomes and selects at most one supported comparison: obligation-framed versus chosen completions, or letting go versus finishing without a release. Each side requires ≥4 reflected evenings, the named feeling ≥3 appearances on the stronger side, and a ≥30 percentage-point gap; mixed obligation/chosen evenings support neither side. The configured AI receives only that winning aggregate—not dates, task text, identifiers, the full distribution, or alternate candidates—and phrases one JSON sentence under 24 words. Client guards reject replies that are overlong, clipped, causal, multi-feeling, or swap the supplied contexts. If no relationship qualifies, the 30-day count remains alone. Browser coverage proves count-only abstention, the selected relationship/privacy boundary, and withholding of the reported clipped shape. Calendar-relative sync fixtures replace dates that naturally expired during testing. No stored reflection schema, retention, sync format, provider, or routing changed. |
| **2.90.6** | **Fix: poem sharing consistency and duplicate export.** Client and edge poem selection now derive the day index from UTC applied to the requested date's components, so shared-page Open Graph metadata names the same poet the page shows in every timezone. The generated share image no longer includes the TODAY mark. A share-in-progress lock prevents rapid repeated taps on “save as image” from exporting twice. No corpus, rotation pool, or stored data changed. |
| **2.90.5** | **Poem corpus: Richard Watson Gilder's 'Because the Rose Must Fade'.** Can approved the complete six-line closing stanza: *"Ah, yes, because the rose / Fades like the sunset skies; / Because rude winter blows / All bare, and music dies— / Therefore, now is to me / Eternity!"* Added year-round as the concrete, impermanence-shaped companion to the Khayyám and Dickinson present-moment selections. Wording verified against *The Poems of Richard Watson Gilder* (1908), p. 232; Gilder died in 1909. Corpus 134 → 135 (W15 / Sp18 / Su12 / Au13 / year-round 77). Smoke coverage pins the exact stanza and six-line ceiling. |
| **2.90.4** | **Poem corpus: Emily Dickinson's 'Forever is composed of Nows'.** Can chose the four-line opening as the closest sister poem to Le Gallienne's Khayyám line about "Here and Now." Added as year-round: *"Forever is composed of Nows — / ’Tis not a different time, / Except for infiniteness / And latitude of home."* The poem was not already present, though three other Dickinson selections were. Wording verified against the Academy of American Poets public-domain text from *The Further Poems of Emily Dickinson* (1929); Dickinson died in 1886. Corpus 133 → 134 (W15 / Sp18 / Su12 / Au13 / year-round 76). Smoke coverage pins the exact author and four-line selection, not merely Dickinson's already-existing presence. |
| **2.90.3** | **Fix: HOW DAYS FELT no longer remains count-only after enough reflections.** The v2.82.0 component-boundary migration moved the Connections AI helpers behind `Today.use('connections')` but left `reflections.js` gating those calls on the removed `_aiIsConfigured`, `_aiGetKey`, and `_aiGetProvider` globals; its response parser was also an unowned removed global. The first false guard prevented auto-reflection from starting, and the later guards would have prevented the request and result even if reached. Reflections now use the Connections component API directly and own their small plain-text response parser. A browser regression seeds 16 varied evenings plus a configured provider and proves the Memory block enters `reflecting…`, sends aggregate-only counts with no dates, and replaces the count-only state with the returned reflection. No routing, provider selection, prompt, stored data, or sync format changed. |
| **2.90.2** | **Fix: morning nudge freeze across days.** `day_nudge_ai` in the Dropbox payload carried no date field, so when Device B synced on Day D+1 it wrote Day D's AI text to `day_nudge_ai_<D+1>` and called `checkDayNudge()` — which found a valid cache and rendered it immediately, blocking fresh generation. That same stale text then re-uploaded, perpetuating the freeze indefinitely (Friday's line persisting through Sunday was a confirmed instance). Fix: added `day_nudge_ai_date: _localISO()` to the upload and gated the restore with `data.day_nudge_ai_date === _todayISO`. Old Dropbox files without the field get `undefined ≠ today` → skip → regenerate fresh. `isReviewFresh` tightened from "not today" to "specifically yesterday" so stale day-reviews don't show as "Yesterday:" after 2+ days. Covered by a new `dropbox-test` assertion (stale date rejected, undated rejected, same-day accepted). |
| **2.90.1** | **Fix: check-path scroll holds at lockY after nudge.** `_doUnfix()` called `scrollTo(0, lockY)` synchronously and it worked — but `_forceRepaint` timers (Dropbox wake handler, 500ms and 1500ms passes) captured `window.scrollY` and re-issued `scrollTo(savedScrollY)` after focus exited, cementing the pre-nudge position. Fix: new `window._focusJustExited` flag is set in `_doUnfix()` and cleared after 2000ms; `_forceRepaint` returns early while the flag is live. `component-contract-test` ceiling 118 → 120 (two `window._focusJustExited =` assignments). The bug only surfaced on the check path because `_onWake()` was called at device-wake time and its 500ms/1500ms timers outlived the focus-close window. |
| **2.90.0** | **Stage 3: `search_trello` tool added to task enrichment agent.** When a Trello board is connected, `task-enrich.js` (Netlify) now declares `search_trello` as a custom tool alongside the existing `web_search_20260209` server tool. Claude checks Trello first (per system prompt) and falls back to web search. Custom tool execution: the function handles `stop_reason === 'tool_use'` by calling `GET /1/search?modelTypes=cards&idBoards={boardId}&cards_limit=5`, serialises up to 5 results (name + due date + shortUrl), and injects a `tool_result` block before continuing the turn. `pause_turn` (server tool) and `tool_use` (custom tool) are now handled in separate branches. MAX_TURNS 3 → 5. Client passes `trelloToken` + `trelloBoardId` from localStorage on every enrichment call. |
| **2.89.1** | **Fix: focus-check fade is now smooth.** Removing `.focusing` immediately when the user checks a focused task lets `_applyDoneStyles`'s `opacity 200ms ease-out` fire from t=0 rather than at 200ms; previously `opacity: 1 !important` blocked the inline style while `.focusing` was active, producing a visible snap. Deferring the timer-to-body relocation from t=0 to the 200ms cleanup callback eliminates the 260px layout jump on tasks below the focused one — the timer now collapses via its own CSS transition (`--dur-fast`, 150ms) before being reparented. The ESC/click-outside path is unchanged. |
| **2.89.0** | **12d Phase B: per-item revoke in the KNOWN panel.** Each returning-task and open-obligation entry in the Memory panel's KNOWN block now has a "dismiss" button. The dismissal is persisted in the new `appMemory.revokedKnownItems` set (`rt:<taskId>` and `oh:<date>|<text40>` keys) so it survives panel re-opens and `_updateReturningTasksMemory` rebuilds. Returns tasks are filtered at write time in `_updateReturningTasksMemory`; obligation entries are filtered at render time. The set is union-merged across devices in `_mergeAppMemory` (revocations never shrink by sync) and wiped when a `clearedAt` watermark is adopted. "Clear all memory" resets it. Component-contract-test: `memory.item-revoke` declared + registered. |
| **2.88.0** | **Reviewed ordinary-time poem round.** Added Can-approved selections to `assets/poems.js`: the complete final stanza of Enrique González Martínez's 'To the Traveller' in Alice Stone Blackwell's 1929 translation; Bhartrihari's complete *Vairāgya Śataka* verse 79 in B. Hale Wortham's 1886 prose translation, reflowed without rewriting to six displayed lines; and Rabindranath Tagore's complete *Stray Birds* 55, reflowed to two lines. Each author and translator died before 1956; exact text was verified against the cited public-domain editions. All three are year-round: their evening, moon, rest, and vine imagery does not establish a season. Corpus 130 → 133 (W15 / Sp18 / Su12 / Au13 / year-round 75). Smoke coverage locks the new voices, count, line ceiling, and tally. No storage or sync interfaces changed. |
| **2.87.0** | **12e: verdicts are permanent — `kindVerdicts` + RETIRED block.** Can: *"do it, we need an evolving ai"* on the v2.86.0 caveat that reactions aged out with the 30-day `spokenLines` window. New slot `appMemory.kindVerdicts` `{ [kind]: { landed, missed, retired, updated } }`, tallied by `_memoryTallyReaction` from every reaction change (switching moves the count); two misses set `retired`, and it lifts when the misses drop below two. `_observationGateExplain` reads it first (`retired by you on <date>`), the 30-day line reactions second. `_memoryRestoreKind` is the only way back: Memory panel **RETIRED** block lists retired kinds with the day and count and a per-item `bring back` (`memory.kind-restore` UI action; `typeBlock` items gained an optional `action`). Cleared by "clear all memory"; synced newer-`updated`-wins, watermark-aware. Tests: pool (87), insights 6f, memory-panel RETIRED + clear, dropbox merge. |
| **2.86.0** | **12e: reactions — the usefulness gate is the person.** Two-state reaction on every spoken line: `landed` / `not really`. Can, after the v2.85.0 cut: a code gate for usefulness needs his feedback, and today was the case for it. Tap the sentence to reveal the two states (About: Sunday, Monday, Today blocks, via `_summaryHTML`/`_reactionHTML` and one delegated listener in `about.js`; the morning strip: first tap reveals a sibling `#dayNudgeReact` instead of dismissing, a state records and dismisses, a second tap on the sentence dismisses without a verdict — sibling because the strip is a `<button>`). Stored on the `spokenLines` entry itself: `reaction`, `reactedAt` (`_memoryReactToLine` in `insights.js`, same state again clears). Rendered only when a spokenLines entry exists for that surface today — rule-based fallback lines were never said and get no control. **Gate:** `_observationGateExplain` doubles a kind's cooldown after one `missed` and retires it (`marked as not landing twice`) after two; `landed` never blocks. Never sent to the model. Sync: on a `date|surface` collision the entry with the newer `reactedAt` wins. Memory panel SAID rows show `you said: …`. Clearing memory clears reactions with the lines. Not wired: Noticed (rule-based, several lines a day, no spokenLines entry) and the focus question. Tests: pool-test (84), insights-test 6e, about-test 13b, nudge-test 6b, dropbox-test merge assertion. |
| **2.85.0** | **The four statistical week kinds are retired; Sunday is outcome kinds only.** Second Phase 4 sample (Sunday 2026-09-07): *"You hit a 6.6 completions/day stride on 5 focus days this week, compared to just 0.5 on the other 2."* Can: *"absolutely useless… you know our northstar."* The line came from `focus-leverage` at its maximum score (130), outranking every commitment observation. Its subject is completions per day — a productivity stat, which the north star rules out in its first paragraph. The four pre-existing kinds (`focus-leverage`, `habit-alignment`, `recurring-day`, `bursts`) were carried into the pool from the pre-north-star week reflection with the highest base scores and never re-judged; the 2026-09-01 candidate table already called `bursts` "same container-subject shape as the cuts". Removed `_buildWeekCandidates` and `_buildWeekReflectionInsight` from `week-reflection-policy.js`; `_buildObservationCandidates` reads `taskOutcomes` only. `_pickSundayInsight` returns null when the pool is missing or throws — silence is the floor, the block hides. `WEEK_REFLECTION_POLICY` bumped to `earned-v2` so today's cached stat line is discarded. `week-reflection-unit-test.mjs` deleted; `observation-pool-test` now asserts no kind is a productivity stat (80 tests); `about-test` Sunday cases rewritten. Not a prompt change: the model phrased the evidence it was given. |
| **2.84.1** | **Fix: `return-finished` links older rows now, not in six weeks.** v2.84.0 stamped `key` only on new rows and said the 45-day window would heal the rest — true, but it left the kind silent on loops that had already closed. `_memoryStampOutcomeKeys()` gives older rows the hash retroactively: `txt_…` rows keyed to themselves (the id is the text identity), live-id rows keyed from the text if a list still holds it, `bf_…` rows left alone (no text ever existed). Cannot run at page load — task lists are not loaded yet — so it runs where the pool runs: top of `_memoryForAI()` and both pool call sites; idempotent, saves only on change. Two pool tests pin the linking boundary: a backfilled done row (hash as id, no key) *must* link via `key \|\| id`; an old live-id done row without key *must not* — so nobody later "fixes" it by matching task names and links two different tasks worded the same. |
| **2.84.0** | **Feat: `return-finished` — the completion side of the let-go loop; Noticed rule 5 retired.** New pool kind (score 92, cooldown 30, Sunday-only by eligibility): links release → return → done per task over 45 days, fires at 3+ distinct returned tasks. All finished → *"Over 45 days, 3 things you had let go came back — …. All 3 got done."* / *Let go, brought back, finished.* Some finished → *"…; 1 of them got done."* / *Brought back is not the same as finished.* None finished → silence (verdict-shaped). When it fires, `letgo-return` is withheld in the same build — same revives, sharper reading. **Linking fix:** done rows are keyed by the live task id while let-go/revive rows use the text hash, so the loop could not close by id; `_memoryRecordOutcome` now stamps `key` (the hash) on every row and the kind links on `key \|\| id`. Older rows lack it and simply do not link — the window heals it, no migration. **Retired:** Noticed rule 5 *"Brought back, and finished — …"* (v2.35.2) — a same-day fact about a task the user had just completed; Can: *"I am aware, it's a fact, but anything for me?"* `n.revivedDone` left inert. Ten pool tests incl. done-before-revive, wrong-key, dedup, eligibility, cooldown; two `key` assertions in insights-test. |
| **2.83.1** | **Quiet runtime cleanup and token parity — no visible or data-interface change.** Removed the unreachable standalone AI sheet completely: its `#aiPanel`/backdrop markup, sheet-only CSS, inline `_aiPanelOpen`/Escape state, badge state, controller/thread/request state, sheet rendering/action handlers, and dead nudge badge path are gone. `assets/assistant.js` now owns only the reachable post-add suggestion lifecycle and fell from 1,514 lines / 63.0 KB to 416 lines / 14.9 KB; assistant coverage now locks both live behavior and sheet absence. Removed the unconditional `renderTrello()` from the seven-second `syncAll()` tick: Trello network changes, sync merges, focus/order mutations, and other actual state owners retain their renders, while age buckets explicitly reconcile at a genuine day boundary and once on wake. Dropbox browser coverage proves unchanged ticks render zero times and day/wake boundaries reconcile once. The opener's computed `:root` palette is now authoritative for task-stat canvas and focus/meeting PiP documents through `_cssToken()`/`_pipTokens()`; eight isolated-document tokens preserve the exact prior values, and new `token-parity-test.mjs` guards 13 canonical mappings plus poem/offline first-paint copies. The component compatibility ceiling drops 123 → 117; its assignment scanner no longer mistakes `===` reads for writes. Default gate: design lint plus 34 non-live suites, 35 checks total. Updated component, AI, Connections, sync, token, accessibility, performance, test, README, version, cache, and rule documentation. No local-storage format, Dropbox payload, Trello API behavior, AI provider routing, or reachable UI changed. |
| **2.83.0** | **Feat: new-day settle.** At the midnight boundary the list snapped — `checkNewDay()` ran `applyNewDayCleanup()` then `renderManual()`, so done rows vanished into Past and every age bucket stepped to its next opacity in one frame — while the date tag (v2.82.2) crossfaded. Now the list catches up on the same beat. `day-lifecycle.js` gains three helpers: `_newDaySnapshot()` captures each manual row and its bucket before the cleanup; `_newDayCollapse(snap, then)` fades and collapses the rows no longer in `manualTasks` (height, padding, opacity → 0 over `--dur-slow` / `--ease-out`, `fill: forwards`) and calls `then` when the last finishes; `_newDaySettle(snap)` runs after the re-render and, for each row whose bucket changed, eases opacity from the old value to the new over 1.2s — deliberately long, because this is the only time age dimming is ever seen as motion. The old opacity is read from the stylesheet by briefly restoring the old bucket with the attribute transition muted, so the numbers stay in one place. Both beats are WAAPI: midnight often coincides with a wake repaint whose display toggle restarts CSS animations from keyframe 0. Reduced motion skips both; the no-graduation and no-change paths cost nothing. `checkNewDay()` snapshots, cleans up, then renders through the collapse callback. Trello rows keep their own per-tick aging. `day-lifecycle-test` 13 seeds an 8-day-old row and a done row: collapse in flight at 120ms, re-render after ~300ms, done row gone, changed bucket easing mid-way at ~500ms, settled to 0.35 with no animations left at 1.8s. |
