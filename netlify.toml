[build]
  publish = "."
  ignore = "git diff --quiet HEAD^ HEAD -- index.html netlify.toml"

[build.processing]
  skip_processing = false

# Production — master only
[context.production]
  command = ""

# dev branch — builds and deploys to https://dev--today-here.netlify.app/
[context.dev]
  command = ""
