{
  "name": "today-scripts",
  "private": true,
  "description": "Dev-only tooling for TODAY — not part of the app",
  "dependencies": {
    "axe-core": "^4.13.0",
    "puppeteer-core": "^23.0.0"
  }
}
